import { createMuiTheme } from "@material-ui/core";
const theme = createMuiTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#C6B28E",
      dark: "#616161"
    },
    secondary: {
      main: "#4D4D4D",
      light: "#D6CKBC"
    }
  }
});
export default theme;
