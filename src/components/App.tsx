import { CssBaseline } from "@material-ui/core";
import * as React from "react";
import LoginPage from "./LoginPage";

export default () => (
  <div>
    <CssBaseline />
    <LoginPage />
  </div>
);
