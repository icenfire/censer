export default (theme: any) => ({
  root: {
    ...theme.flexColumnCenter,
    justifyContent: "flex-start",
    height: "100%",
    width: "100%",
    fontWeight: 400,
    paddingTop: "1.5rem"
  }
});
